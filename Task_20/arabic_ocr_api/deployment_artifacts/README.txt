Copy the three files produced by the training notebook's
"Save the Best Model" cell into this folder:

  - crnn_ocr_model.keras
  - vocab.json
  - config.json

They are generated together into ./deployment_artifacts/ by the notebook,
so you can just copy that entire folder here and overwrite this one.
